open the project in VS Code, in terminal type "npm install" to install node modules, then run "npm start"
- clone the API project https://github.com/MatheusLaurindo/ToDoList.WebAPI and run it in visual studio
